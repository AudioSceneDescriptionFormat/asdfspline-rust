language: rust
rust:
  - stable

cache: cargo

script:
  - cargo build --verbose --all
  - cargo test --verbose --all
  - cargo doc --verbose --all

branches:
  only:
    - master

notifications:
  email: false
