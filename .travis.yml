language: rust
rust:
  - stable

cache: cargo

before_script:
  - rustup component add rustfmt

script:
  - cargo build --verbose --all
  - cargo test --verbose --all
  - cargo doc --verbose --all
  - cargo fmt --all -- --check

branches:
  only:
    - master

notifications:
  email: false
